import 'package:flutter/widgets.dart';

class UmlDesign extends StatelessWidget {
  const UmlDesign({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}