class SplashConstants {
  static const animationDuration = Duration(milliseconds: 1100);
  static const navigationDelay = Duration(seconds: 3);
  static const logoAnimation = Duration(milliseconds: 1800);
  static const progressAnimation = Duration(milliseconds: 1400);
  static const floatingAnimation = Duration(seconds: 8);
}
